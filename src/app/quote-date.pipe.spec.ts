import { QuoteDatePipe } from './quote-date.pipe';

describe('QuoteDatePipe', () => {
  it('create an instance', () => {
    const pipe = new QuoteDatePipe();
    expect(pipe).toBeTruthy();
  });
});
